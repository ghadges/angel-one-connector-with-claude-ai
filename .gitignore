# Vendored at build time — never commit
server/node_modules/

# Build output — ships as a GitHub Release asset, not in the repo
*.mcpb

# Never commit credentials
claude_desktop_config.json
credentials.*
.env
.env.*
*.key

# OS / editor noise
Thumbs.db
.DS_Store
*.swp
*.bak
